<div class="container">
    <div class="row">
        <div class="col-8 offset-2">
            <div class="card mt-4">
                <div class="card-body">
                    <h5 class="card-title">Example</h5>
                    <p>
                        <strong>city</strong>: {{activitiesData.id}}</p>
                    <p>
                        <p>
                            <strong>Text</strong>: {{activitiesData.category}}</p>
                        <p>
                            <strong>Description:</strong> {{activitiesData.price}}</p>
                        <a href="/">← Back To Home</a>
                </div>
            </div>
        </div>
    </div>
</div>